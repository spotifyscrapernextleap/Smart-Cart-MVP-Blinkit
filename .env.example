# Committed template. Copy to .env.local and fill in. (Build spec §7.2)
#
# Read ONLY inside src/app/api/recommend/route.ts. Never referenced in a client
# component, and never prefixed NEXT_PUBLIC_ — that would ship the key in the
# browser bundle. (EDGE_CASES E1)
GROQ_API_KEY=
